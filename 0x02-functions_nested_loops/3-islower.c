#include "main.h"

/**
 * _islower - it range lower case
 *
 * @c: parameter tp print
 * Return: 1 if it lower case
 * and 0 otherwise
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
		return (1);
	else

		return (0);
}
